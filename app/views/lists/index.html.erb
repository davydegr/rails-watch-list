<h1>Overview of lists</h1>

<h2>Add a list</h2>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit %>
<% end %>

<h2>Your lists</h2>
<% @lists.each do |list| %>

    <%= link_to list_path(list) do  %>
      <p><%= list.name %></p>
    <% end %>

<% end %>
